apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-analytics-api.fullname" . }}
  labels:
    {{- include "event-analytics-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-analytics-api.selectorLabels" . | nindent 4 }}
